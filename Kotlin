/**
 * You can edit, run, and share this code.
 * play.kotlinlang.org
 */
fun somar():Int {
    return 1 + 1
}

fun main() {
    
    //Criação de variaveis    
    var nome: String = "Cleiton"
    val sobrenome: String = "Migliorini"
    var idade = 20
    lateinit var depoisInicio
    
    //Criacao de listas imutavies
    val listaFormaPagamento = listOf("Pix", "Boleto")
    val listaNomes = mutableListOf()
    listaNomes.add("Cleiton")
    
    if(idade >= 18) {
        println("Maior de idade")
    } else {
        println"Menor de idade")
    }
    
    when(idade)
    	18 -> "Maior de idade"
    	20 -> "Idade igual a 20"
    else -> "Outra Udade"
    
    // Null Safety
    // Operador ?
    //Elvin
    
    var tamanhoLista = listaNomes?.length() ?: "Sem itens"
    
    
}
